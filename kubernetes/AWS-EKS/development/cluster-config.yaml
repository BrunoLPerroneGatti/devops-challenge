apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: devops-challenge
  region: us-east-2

iam:
  withOIDC: true

autoModeConfig:
  enabled: false

managedNodeGroups:
  - name: workers
    instanceType: t3.small
    minSize: 2
    maxSize: 3
    desiredCapacity: 2